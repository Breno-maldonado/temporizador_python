# ⏱️ Temporizador em Python

Este projeto é um **temporizador simples em Python** que realiza uma **contagem regressiva em segundos** e exibe o tempo restante no formato `MM:SS` até o tempo acabar.

---

## 📌 Funcionalidades

- Recebe um tempo em segundos via entrada do usuário
- Valida se a entrada informada é numérica
- Converte automaticamente o tempo para minutos e segundos
- Atualiza o tempo a cada segundo no terminal
- Exibe uma mensagem ao final da contagem regressiva

---

## ⚙️ Como Funciona

1. O usuário informa o **tempo total em segundos**.
2. O sistema valida se a entrada é válida.
3. O tempo é convertido para o formato `MM:SS`.
4. O temporizador realiza a contagem regressiva, atualizando o terminal a cada segundo.
5. Ao final, uma mensagem de conclusão é exibida.

---

## ▶️ Como Executar

1. Certifique-se de ter o **Python 3.8 ou superior** instalado.
2. Clone ou baixe este repositório.
3. No terminal, navegue até a pasta do projeto.
4. Execute o comando:

```bash
python temporizador.py
